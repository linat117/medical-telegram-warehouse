name: "medical_telegram_warehouse"
version: "1.0"
config-version: 2

profile: "medical_warehouse"

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  medical_telegram_warehouse:
    +schema: raw
    +materialized: table
